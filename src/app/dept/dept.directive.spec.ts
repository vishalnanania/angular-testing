import { DeptDirective } from './dept.directive';

describe('DeptDirective', () => {
  it('should create an instance', () => {
    const directive = new DeptDirective();
    expect(directive).toBeTruthy();
  });
});
